define(function( require ) {
    var helper = require( 'saber-control/helper' );

    describe( 'Helper', function() {

        describe( 'basic', function() {

            it( '`getGUID`', function() {
                expect( true ).toBe( true );
            });

            it( '`getId`', function() {
                expect( true ).toBe( true );
            });

            it( '`dispose`', function() {
                expect( true ).toBe( true );
            });

        });

        describe( 'class', function() {

            it( '`getPartClasses`', function() {
                expect( true ).toBe( true );
            });

            it( '`addPartClasses`', function() {
                expect( true ).toBe( true );
            });

            it( '`removePartClasses`', function() {
                expect( true ).toBe( true );
            });

            it( '`getStateClasses`', function() {
                expect( true ).toBe( true );
            });

            it( '`addStateClasses`', function() {
                expect( true ).toBe( true );
            });

            it( '`removeStateClasses`', function() {
                expect( true ).toBe( true );
            });

        });

        describe( 'dom event', function() {

            it( '`addDOMEvent`', function() {
                expect( true ).toBe( true );
            });

            it( '`removeDOMEvent`', function() {
                expect( true ).toBe( true );
            });

            it( '`clearDOMEvents`', function() {
                expect( true ).toBe( true );
            });

        });

    });
});